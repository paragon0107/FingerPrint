spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://fingerprint.cdsc42kaaniv.ap-northeast-2.rds.amazonaws.com:5432/postgres
    username: postgres
    password: fingerprint

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: false
        show_sql: false
        dialect: org.hibernate.dialect.PostgreSQLDialect

  threads:
    virtual:
      enabled: true